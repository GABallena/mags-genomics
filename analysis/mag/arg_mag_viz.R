#!/usr/bin/env Rscript
# Portfolio-safe stub.
#
# This repository uses a consolidated analysis script to generate
# ARG→MAG visualizations. This older entry-point is retained so legacy
# calls do not fail.
message("[DEPRECATED] arg_mag_viz.R → use the consolidated abstract analysis script for ARG→MAG plots.")
message("Working directory: ", getwd())
quit(status = 0)
